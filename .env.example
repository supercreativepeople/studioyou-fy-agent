# LiveKit Cloud project (confirmed live on Cloud Run studioyou-api 2026-06-15)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Claude — same key as studioyou-api's ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=

# Tavus — same key as studioyou-api's TAVUS_API_KEY
TAVUS_API_KEY=

# Stock Wizard-of-Oz replica (Raj), validated Session Q
TAVUS_REPLICA_ID=rf8f3aa4b33e

# LiveKit-mode persona (pipeline_mode=echo, transport_type=livekit)
# Created 2026-06-15. Rendering-only — no system_prompt (Claude owns that).
TAVUS_LIVEKIT_PERSONA_ID=pb0277f1cfc1

# STT/TTS handled via LiveKit Inference (deepgram/nova-3, cartesia/sonic)
# using the LIVEKIT_* credentials above — no separate keys needed.
